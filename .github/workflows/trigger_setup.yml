name: Remote Setup Trigger

on:
  push:
    paths:
      - 'trigger.txt'

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up and Wait for Codespace
        run: |
          # 1. まずCodespaceを強制的に「起動」させる
          NAME=$(gh codespace list --json name -q ".[] | select(.name | contains(\"miniature-telegram\")) | .name")
          echo "Waking up Codespace: $NAME"
          gh codespace wakeup -c "$NAME"
          
          # 2. 「Available」になるまで最大5分間（30回）じっくり待機
          for i in {1..30}; do
            STATUS=$(gh codespace list --json name,state -q ".[] | select(.name | contains(\"miniature-telegram\")) | .state")
            if [ "$STATUS" == "Available" ]; then
              echo "Codespace is now Available!"
              exit 0
            fi
            echo "Current status: $STATUS... waiting 10s (Attempt $i/30)"
            sleep 10
          done
          echo "Codespace failed to wake up"
          exit 1
        env:
          GH_TOKEN: ${{ secrets.MY_TOKEN }}

      - name: Start Services and Force Public
        run: |
          NAME=$(gh codespace list --json name -q ".[] | select(.name | contains(\"miniature-telegram\")) | .name")
          
          echo "Starting setup script via SSH..."
          # 裏側でFirefoxなどを起動
          gh codespace ssh -c "$NAME" -- -o StrictHostKeyChecking=no "nohup bash /workspaces/dakion/.devcontainer/setup.sh > /dev/null 2>&1 &"
          
          echo "Waiting 20 seconds for services to settle..."
          sleep 20
          
          echo "Setting port 8080 to PUBLIC..."
          # 最後にポートを全体公開にする
          gh codespace ports visibility 8080:public -c "$NAME"
        env:
          GH_TOKEN: ${{ secrets.MY_TOKEN }}
